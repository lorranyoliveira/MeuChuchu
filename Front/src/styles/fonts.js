import metrics from './metrics'; 

const fonts = {
    base: metrics.screenWidth*0.06666,
    button: metrics.screenWidth*0.05,
    bar: metrics.screenWidth*0.04,
    title: metrics.screenWidth*0.083333,
    modalTitle:(metrics.screenWidth * 0.083333)/1.4,
};

export default fonts;